{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    },
    "MailSettings": {
      "Mail": "iangatumu@gmail.com", // Do not use a personal Email ID, quite risky if compromised // Used to send a mail from.
      "DisplayName": "Mail", // the Name that appears to the receiver.
      "Password": "mbsw atni xinf olqe",
      "Host": "smtp.gmail.com", // (Gmail) allows you to send 2,000 emails per day using the Gmail SMTP Server for FREE
      "Port": 587 // Use 465 (SSL) or 587 (TLS) for Gmail
    }
  }
}
